<%= form_with(model: system_config, local: true) do |form| %>
  <% if system_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(system_config.errors.count, "error") %> prohibited this system_config from being saved:</h2>

      <ul>
      <% system_config.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :system_config_name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_area :value, id: :system_config_value %>
  </div>

  <div class="field">
    <%= form.label :data_type %>
    <%= form.text_field :data_type, id: :system_config_data_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

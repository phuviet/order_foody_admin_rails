<%= form_with(model: shop_detail, local: true) do |form| %>
  <% if shop_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shop_detail.errors.count, "error") %> prohibited this shop_detail from being saved:</h2>

      <ul>
      <% shop_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key, id: :shop_detail_key %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_area :value, id: :shop_detail_value %>
  </div>

  <div class="field">
    <%= form.label :deleted_at %>
    <%= form.datetime_select :deleted_at, id: :shop_detail_deleted_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: version, local: true) do |form| %>
  <% if version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(version.errors.count, "error") %> prohibited this version from being saved:</h2>

      <ul>
      <% version.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_type %>
    <%= form.text_field :item_type, id: :version_item_type %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id, id: :version_item_id %>
  </div>

  <div class="field">
    <%= form.label :event %>
    <%= form.text_field :event, id: :version_event %>
  </div>

  <div class="field">
    <%= form.label :whodunnit %>
    <%= form.text_field :whodunnit, id: :version_whodunnit %>
  </div>

  <div class="field">
    <%= form.label :object %>
    <%= form.text_area :object, id: :version_object %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
